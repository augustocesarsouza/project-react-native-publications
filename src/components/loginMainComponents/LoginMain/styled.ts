import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  containerMain: {
    alignContent: "center",
  },

  reactLogo: {
    width: 40,
    height: 40,
  },

  headerLogin: {
    fontSize: 20,
    fontWeight: "bold",
    color: "red"
  }
});
